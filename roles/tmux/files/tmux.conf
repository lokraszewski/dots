# Terminal — truecolor + nerd font glyph support
set -g default-terminal "tmux-256color"
set -as terminal-overrides ",*:Tc"
set -as terminal-features ",*:RGB"
set -gq allow-passthrough on

# Prefix
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'fabioluciano/tmux-powerkit'
set -g @powerkit_plugins "group(cpu,memory,loadavg),group(git,github),group(kubernetes,cloud),battery,group(hostname,ssh)"

set -g @powerkit_theme "catppuccin"
set -g @powerkit_theme_variant "frappe"
set -g @powerkit_transparent "false"

# Choose your separator style
set -g @powerkit_separator_style "rounded"

# Customize update interval
set -g @powerkit_status_interval "5"


# Initialize TPM (keep this line at the very bottom)
run '~/.tmux/plugins/tpm/tpm'


